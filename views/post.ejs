<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <a href="/feed" class="btn btn-default btn-sm">Feed</a>
    <a href="/profile" class="btn btn-default btn-sm">Profile</a>
    <div class="ticket">
        <a href="/post?id=<%=post._id%>"><img class="picture" src="<%=post.image %>"></a>
        <span class="caption"><%= post.caption %></span>
        <% for(i = comments.length-1; i > 0; i--) { %>
            <% if (comments[i].post == post._id) { %>
                <p class="comment"><%= comments[i].comment%> | <span><%= comments[i].timestamp %></span></p>
            <% } %>
        <% } %>
    </div>
</body>
</html>